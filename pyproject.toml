[project]
name = "searchthearxiv"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.36,<3",
    "pinecone>=6,<7",
]

[project.optional-dependencies]
app = [
    "Flask>=3.1,<4",
    "requests-html==0.10.0",
    "lxml>=6.1,<7",
    "lxml_html_clean",
    "validators>=0.35,<1",
    "gunicorn>=26,<27",
]
data = [
    "tiktoken>=0.12,<1",
    "tqdm>=4.67,<5",
    "kaggle>=2.1,<3",
]

[dependency-groups]
dev = [
    "ruff>=0.11,<1",
]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort (import sorting)
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "ANN",   # flake8-annotations (require type hints)
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "TCH",   # flake8-type-checking
    "T201",  # flake8-print (no print statements)
    "RUF",   # ruff-specific rules
]
ignore = [
    "ANN204",  # missing return type annotation for special method
]
per-file-ignores = {"data/update_kaggle.py" = ["T201"]}

[tool.ruff.lint.isort]
known-first-party = ["app", "data"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
